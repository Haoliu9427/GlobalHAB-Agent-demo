# HF3.9.3 名称同步修复
已只读核实GitHub main分支的app.py和两个页面模块均已是HF3.9.2及新名称。截图与仓库不一致，不应再据此认定用户未上传页面模块。
本次修复让研究页面从入口所在目录读取当前源码，并在隔离命名空间统一名称，避免复用旧的已导入显示模块；不修改计算模块。
可以仅替换仓库根目录app.py，已有Secrets保留。随后在云端应用Reboot，并确认运行版本HF3.9.3-LABEL-SYNC-20260920。
本地已测试“新入口 + 旧页面源码 + 预先导入旧模块”及两种入口切换，名称与选择框均正确。实际云端重启状态仍需用户确认。
