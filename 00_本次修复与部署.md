# HF3.9.6 图表对齐与自主接入

## 本次修改
- 实验对照图使用等宽两列，相同图高、绘图区边距与横轴范围，候选选择放在两图上方，数据明细放在两图下方。分别标识独立测试集和验证集。
- 自主接入使用表单：填写地址、模型名和密钥，再点击“保存并检查连接”，通过后启动研究。无需在 Secrets 重复配置。
- 连接错误区分 HTTP 401、402、403、404、429 及网络失败；不向页面返回原始服务错误正文或密钥。
- 保留 HF3.9.5 结果同步修复。

## 部署
覆盖仓库根目录内容，确保 app.py、src/globalhab_demo/scientific_agent_ui.py、src/globalhab_demo/scientific_agent_results.py 更新。
在 Streamlit 执行 Reboot app，版本应为 HF3.9.6-CHART-ALIGNMENT-20260920。现有 Secrets 无需修改。重启会清空未下载的会话记录。

## DeepSeek 自主接入
地址：https://api.deepseek.com
模型：deepseek-flash（以服务商当前可用模型为准）
密钥：DeepSeek 提供的 API Key，直接填入密码框，不使用 ModelScope Token。
来源：https://api-docs.deepseek.com/zh-cn/

## 验证范围
浏览器实测 1600 像素视口：两图顶边坐标相同，均为 640×370；412 像素手机视口无页面横向溢出。
无 Secrets 的自主接入表单测试通过；通过模拟服务响应验证 DeepSeek 请求路径、参数、成功解锁研究以及 401/402/404/429/503 失败处理。未使用用户 DeepSeek 密钥，未验证其账户余额或线上连通性。
真实历史模型结果回放通过：三图、记录下载、同步模型解读和自动选择最新证据。
