# HF3.9.5 结果发布修复

本次直接修改 scientific_agent_ui.py：结果发布在页面模块内完成，不再导入 result_pool.publish_scientific_result。
app.py 恢复普通模块调用，移除动态源码替换；未修改模型规划、实验计算或结论。

## 部署
1. 把本包根目录内容覆盖到 GitHub 仓库根目录，尤其是 app.py 和 src/globalhab_demo/scientific_agent_ui.py。不要上传为额外一层文件夹。
2. 等待 GitHub 提交完成，在 Streamlit 应用管理菜单执行 Reboot app（重启应用）。仅刷新浏览器不会可靠清除服务器已加载的旧模块。
3. 运行版本应为 HF3.9.5-RESULT-PUBLISH-20260920。现有 Secrets 不需要修改。
4. 重启会清空会话内未下载的实验记录。新实验完成后应显示图表与下载按钮，并同步至模型解读。

## 已验证
使用之前真实 Qwen 调用保存的完整实验结果，在 Streamlit AppTest 回放；故意加载不含发布函数的旧 result_pool 模块。
三个图表、审计记录下载、完整证据同步、重复刷新不重复入库、模型解读自动选中最新记录均通过。
本次没有额外调用收费或配额模型，也没有直接登录你的 Streamlit 云端进行验证。
