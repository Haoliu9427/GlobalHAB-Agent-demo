# HF3.9.1 请先看这里

1. 将本工程完整内容上传到 Streamlit 对应仓库，入口选择 app.py（streamlit_app.py 也兼容）。
2. 在该应用 Settings → Secrets 添加下面两行，把占位文字换成你的魔搭Token：

```toml
[modelscope]
api_key = "替换为你的魔搭Token"
```

如果已有[modelscope]请修改原段，不要重复添加。其他配置保留。旧[qwen]配置仍兼容。不要将真实Token写入GitHub。工程包不包含真实令牌。
3. 保存后重启应用，在研究验证选择平台模型，即可显示35B、122B、397B三个型号。先检查连接，再开始研究。
4. 右上角运行版本应为 HF3.9.1-CLOUD-CONFIG-20260920。

本版修复的是云端配置易漏填的问题，不会自动把本机凭证上传到云端。支持原HF3.9的15种当前任务预测模型、STS交互适配、模型自主选择和结果图表。地图与情景模拟仍在规则工作区，未新增到大模型结果区。

测试范围：本地Streamlit使用Secrets加载配置并真实调用魔搭模型。不是在你的Streamlit Cloud账户完成部署测试。免费服务可能存在额度限制或空响应，不能保证每次调用成功。
