# GlobalHAB-Agent Case / Evidence Loop

## 1. 目的

本版本用同一个 `Case` 把研究风险候选、现场影像、专业/实验室确认、视觉训练和大模型解释串成一条可追溯证据链。任何下游证据都不会覆盖上游原始指标。

## 2. 闭环

```text
研究与验证
Risk / Route / Lag / Top-k
        ↓
生成现场复核任务（Case）
        ↓
现场影像甄别
照片 + 现场环境元数据
        ↓
质量门控 + 自适应视觉路由
        ↓
Low / Moderate / High / DEFER
        ↓
专业/实验室确认
        ↓
 ┌────────────────┬────────────────┐
 │                │                │
进入研究证据链      进入视觉训练库
 │                │
 │                ↓
 │            轻量头重新训练
 │                ↓
 │            新视觉模型版本
 │
 ↓
大模型综合解释
        ↓
风险 + 视觉 + 环境 + 实验室证据
        ↓
下一步复核建议 / 答辩解释 / 科研摘要
```

## 3. Case 数据结构

每个 Case 保存在 `data/cases/cases.json`，主要包含：

- `research`：候选海区、风险指数、Route、Lag、Top-k 容量与覆盖、情景参数；
- `field_task`：现场拍照/采样任务；
- `visual`：视觉筛查类别、质量、不确定性、模型分支；
- `field_metadata`：时间、位置、水色、异味、泡沫、温盐、DO、Chl-a 等；
- `lab_evidence`：专业人员、显微镜、qPCR、毒素确认；
- `evidence`：按时间追加的证据账本；
- `llm_notes`：大模型解释记录。

## 4. 证据等级

- **C·模型风险候选**：研究与验证生成的候选，只用于安排复核；
- **B·现场视觉筛查**：照片和现场信息形成的视觉证据，不确认具体藻种/毒素；
- **A·实验室/专业确认**：专业人员、显微镜、qPCR 或毒素检测；
- **解释记录**：大模型综合解释，不改变任何证据等级。

## 5. 工作区联动

### 研究与验证 → 现场影像甄别

“风险研判”页新增“生成现场复核任务并前往影像甄别”。当前候选海区、风险指数、Route、Lag、Top20%事件覆盖等写入 Case；影像页自动读取该 Case。

### 现场影像甄别 → 研究证据链

完成筛查后可“登记到当前研究Case”。视觉结果登记为 B 级证据；照片可保存到“我的影像数据”，但默认标签为“不确定”、不自动进入监督训练。

### 专业/实验室确认 → 视觉训练库

确认区支持专业人员、显微镜、qPCR、毒素四种方式。用户给出确认结论与视觉现象标签后，可选择把该照片作为高证据训练样本写入影像库。

### 视觉训练 → 新模型版本

“模型训练与版本”继续使用已确认训练照片训练冻结 backbone 上的轻量分类/元数据融合头。候选模型先留出验证，再由用户显式注册；不会自动覆盖当前模型。

### Case → 大模型综合解释

“大模型结果解读”新增 **当前完整Case（推荐）**。摘要同时包含研究候选、视觉筛查、现场环境与专业/实验室证据。大模型必须指出证据一致性、冲突、缺口和下一步复核；生成后可保存回 Case 的解释记录。

## 6. 隐私与边界

- 原始现场照片不会因为进入大模型工作区而自动发送给远程服务；默认只发送结构化 Case 摘要，并需要用户显式授权。
- 视觉筛查不能自动把模型候选改成真实 HAB 标签。
- 大模型解释不能改变概率、指标、模型权重或事件确认状态。
- 专业/实验室证据仍受采样时间、位置、检测方法与检测限约束。

## 7. 复现检查

```bash
PYTHONPATH=src pytest -q tests/test_case_manager.py
PYTHONPATH=src python scripts/check_case_loop.py
python scripts/verify_release.py
python scripts/verify_package.py
```
