# HF2 部署位置说明

本包是“仓库根目录直接部署版”。解压后应当立刻看到：

- `app.py`
- `streamlit_app.py`
- `requirements.txt`
- `src/`
- `assets/`
- `.streamlit/`

不要把整个解压目录作为一个子文件夹放进旧仓库。否则 Streamlit 仍会执行旧仓库根目录的 `app.py`，页面会继续显示旧版桑基图和折线图。

## Streamlit Community Cloud

1. 用本包内容覆盖仓库根目录，或建立一个新仓库并把本包内容放在根目录。
2. Main file path 选择 `app.py`；也可以选择 `streamlit_app.py`。
3. 重新启动应用并清除缓存。
4. 正确版本特征：
   - 首页默认无左侧控制栏；
   - 顶部工作区导航没有单选圆圈；
   - 首页出现“五步研究思路概览”；
   - “工作区联动”是五个横向卡片，不是桑基图；
   - 南澳事件使用矩形热力图；
   - 挪威前向验证使用热力矩阵。

构建标识：`HF2-20260918`
