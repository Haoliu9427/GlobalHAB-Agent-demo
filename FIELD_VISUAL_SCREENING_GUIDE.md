# GlobalHAB-Agent 现场影像甄别工作区

## 1. 定位

`现场影像甄别 / Field Visual Screening` 是 GlobalHAB-Agent 的低成本现场证据入口。用户可用手机拍摄海面，或上传 JPG/PNG 图片，系统先检查图像质量，再提取透明的颜色与纹理特征，并结合用户填写的现场信息形成**复核优先级**。

该工作区的默认实现是 **规则 + 通用颜色/纹理特征演示基线**，不是已经用藻华照片训练完成的分类器，也不输出藻种、毒素或业务预警概率。设计目标是验证完整工作流：

```text
风险研判 → 到现场/船上拍照 → 视觉初筛 → 是否值得补采 → qPCR/显微镜/毒素/环境数据 → 回到GlobalHAB-Agent复核
```

## 2. 支持的输入

### 图像

- 手机现场拍照（Streamlit camera input）；
- 上传 JPG / JPEG / PNG；
- 单张图片上限 12 MB。

建议让海面占画面大部分，避免逆光，尽量保留原始照片。同一点位建议从不同角度拍摄 2–3 张。

### 现场元数据

- 拍摄日期与时间；
- 海域 / 位置文本；
- 肉眼水色；
- 是否有异味；
- 泡沫、浮膜、漂浮物等表面现象；
- 近期高温背景；
- 鱼贝异常/死亡；
- 可选：水温、盐度、溶解氧、Chl-a 与现场备注。

## 3. 默认视觉基线

默认后端位于：

```text
src/globalhab_demo/field_visual.py
```

它只使用本地、可解释特征：

- 亮度、对比度、过曝/欠曝比例；
- 简单纹理/锐度代理；
- 高亮低饱和区域（可能与泡沫、浪花、反光有关）；
- 绿色、红棕、浑浊样和蓝/青色视觉特征占比。

输出类别包括：

- 正常/未见明显异常；
- 绿色水体异常；
- 红棕色水体异常；
- 高浑浊/泥沙样；
- 表层浮沫/漂浮物样；
- 不确定。

类别名称描述的是**视觉现象**，不是生物分类结果。

## 4. 图像质量检查

系统会提示：

- 分辨率偏低；
- 画面偏暗；
- 过曝；
- 强反光；
- 纹理信息不足/可能模糊；
- 对比度过低。

如果图像质量或拍摄目标不满足基本条件，输出 `DEFER / 需重拍`，不会强行给出高/中/低结果。

## 5. 复核优先级

页面输出 `低 / 中 / 高 / DEFER`。这一等级综合：

1. 照片中的视觉异常特征强度；
2. 图像质量；
3. 用户填写的现场辅助线索。

它表示**进一步现场复核的优先级**，不是 `HAB发生概率`，也不是监管阈值。

## 6. 科学边界

### 可以据此声称

- 照片存在绿色、红棕、浑浊、亮白表层等视觉异常；
- 图像质量是否足以支持低成本初筛；
- 该点位是否值得优先补充采样和实验室证据。

### 不能据此声称

- 不能仅凭普通海面照片确认 Karenia、Alexandrium、Pseudo-nitzschia 或其他具体藻种；
- 不能仅凭照片判断是否产毒或给出毒素浓度；
- 不能把复核优先级写成业务预警概率；
- 不能用“照片看起来正常”排除肉眼不可见但具有生态风险的 HAB；
- 不能自动下达停采、转移、投喂或增氧等运营指令。

## 7. 与大模型结果解读的连接

完成一次影像甄别后，点击：

```text
送入大模型结果解读
```

系统会将当前结果保存在 Streamlit `session_state`，并跳转到“大模型结果解读”工作区，自动选择 `最近一次现场影像甄别`。

**照片本身不会随跳转发送给远程大模型。** 大模型只读取本地生成的结构化文字摘要，而且仍需要用户在大模型工作区显式勾选远程发送授权。

## 8. 后续替换真实视觉模型

代码中定义了 `VisualScreeningBackend` 接口。未来获得带有现场照片与实验室确认标签的数据后，可以加入真正经过训练和校准的视觉模型，例如轻量 CNN / ViT / DINO 特征分类器，而不需要重写 Streamlit 页面或大模型连接逻辑。

替换后仍建议保留：

- 独立时间/地点留出；
- 概率校准；
- 图像质量门控；
- `DEFER` 状态；
- 泥沙、反光、泡沫、海草等混淆因素；
- 藻种与毒素必须由独立证据确认的边界。

## 9. 非 UI 检查

```bash
python scripts/check_field_visual.py
pytest -q tests/test_field_visual.py
```

发布检查：

```bash
python scripts/verify_release.py
python scripts/verify_package.py
```


## 10. 自适应视觉路由升级

决赛增强版在透明规则基线之上加入 `Adaptive Visual Screening Router (AVSR)`：

```text
质量门控 → 自适应路由 → DINOv2 / ConvNeXt / EfficientNet特征
         → 轻量分类头 + 现场元数据 → uncertainty / OOD → DEFER或复核优先级
```

- 颜色异常明确时优先 EfficientNet；
- 浮沫、浑浊或复杂表层纹理优先 ConvNeXt；
- 场景模糊或接近决策边界时优先 DINOv2，并可增加第二分支做一致性复核；
- 图像质量不合格时在模型之前直接 `DEFER / 需重拍`。

发布包不会附带伪造的视觉训练头。只有本地编码器与使用真实标注照片训练/校准的 `.npz` 轻量头同时存在时，深度分支才会参与最终结果。否则页面明确显示“安全回退”，继续使用规则基线。训练接口、资产路径和不确定性定义见 `VISION_ROUTER_GUIDE.md`。
