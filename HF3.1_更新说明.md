# HF3.1 统一底图与导航修复

构建号：HF3.1-MAP-NAV-20260918

1. 联合沙盘地图接回全局底图设置。右上角选择 OpenStreetMap 时使用开放街图，选择内置矢量时使用矩形矢量地图；不再单独使用椭圆投影。所有海区坐标保持不变。
2. 各对象阈值编辑表默认展开。相同阈值时明确说明结果相同的原因，支持根据物种或场站资料分别填写。没有人为编造差异化生物参数。
3. 工作区导航换为房屋、实验烧瓶、分析图、相机和思考图标。搜索放大镜改为矢量图标，与设置控件统一高度并垂直居中。

## 为什么参数扰动结果相同
四类对象共用同一环境，默认三项阈值也相同，因此每行计算完全相同。以截图为例，藻华压力65除以上限60约为1.08；27种组合中18种越过上限，所以约为67%。这表示演示阈值对照，不能据此认为四类生物具有相同敏感性。修改各对象阈值后按各自阈值计算；若扰动均未跨越阈值，百分比仍可能相同。

## 部署
完整覆盖工程并重启，不要只替换 app.py。入口仍为 app.py 或 streamlit_app.py。右上角设置的运行版本应显示上述构建号。本次未修改线上仓库或应用。

## 验证
五项行为测试通过，包含两种底图切换、坐标保留与对象独立阈值。五个工作区 AppTest 通过，捕捞海区切换、移除网箱鱼及空选择处理通过。本地浏览器确认导航图标与搜索/设置布局，图标垂直中心相差不足1像素。原研究 data/ 和 outputs/ 文件哈希保持不变。
历史 validation 文件保留，本轮记录为 hf31_acceptance.json。
