# HF3.6.1 启动稳定性修正版

构建号：HF3.6.1-STARTUP-20260918

## 本次处理
- 关闭生产环境的源码文件监视，避免部署过程中清理正在使用的 Python 模块。
- 删除页面刷新时对共享模块的强制 reload；代码更新通过服务器重启生效。
- 保留 HF3.6 的竖屏布局、相机入口、图片识别反馈和原版首页精修。
- 不改研究数据、模型计算或 APK。

## 部署步骤
1. 用完整部署包覆盖仓库中的工程文件。app.py、src、assets、.streamlit 都应在仓库根目录，不能多套一层文件夹。
2. 特别确认 `.streamlit/config.toml` 的 `[server]` 中包含 `fileWatcherType = "none"`。网页上传时隐藏文件夹可能漏掉，请单独核对。
3. 文件全部更新后，在 Streamlit Cloud 的 Manage app 中执行 Reboot app。仅刷新网页不能保证新配置生效。
4. 等待重启完成，再刷新网页或重新打开 APK；设置中核对构建号 HF3.6.1-STARTUP-20260918。

已经完整安装 HF3.6 的工程也可使用增量补丁，保持补丁内目录结构覆盖文件，然后重启。更早版本请用完整部署包。
以后更新 Python 源码后同样需要 Reboot app；普通按钮操作、参数修改和页面切换仍正常运行。

## 验证与边界
本地 Python 3.12.14 / Streamlit 1.62.0 检查通过：实际读取关闭监视配置、八线程同时导入 real_benchmark、两个启动入口及各三次刷新、五工作区、联合沙盘五项行为测试、快速图片筛查、模拟识别异常提示与旧结果清理。
完整包解压后另做启动检查，压缩包逐文件核验 SHA256，研究数据与原包一致。

截图中的错误发生在 Python 导入器，符合模块缓存被移除时的故障形态。当前已处理工程中的重载风险；由于云端隐藏了 KeyError 的具体键名，尚不能确认服务器上的唯一触发源，也未在用户云端验证修复。若完整更新并重启后仍报错，请保留 Manage app 日志中包含 KeyError 键名的完整报错。不要修改科研数据来绕过启动错误。

本包未自动发布线上。APK 无需重装；三星手机实机相机与深度模型识别仍需在线站点恢复后复测。
