# 安卓 1.2.0 与网页 HF3.6.2 更新说明

## 本次更改
- 删除 APK 底部重复且无效的五个按钮。
- 手机网页的五工作区导航改为上三下二的两行按钮，全部可见，无需左右滑动，按钮高度48像素；仍使用网页自身的工作区切换逻辑。
- 缩小手机页头并取消手机上的固定大页头，避免遮挡内容；桌面导航保留。
- 安卓1.2.0的现场拍照改用系统相机，拍摄确认后通过文件选择回调把照片交给网页识别。
- 系统相机只获得本次照片临时文件的访问权限；不申请相册全盘读写或麦克风权限。取消拍照会取消本次选择。
- 保留 HF3.6.1 的启动修复及原有科研数据、布局、识别进度和错误提示。

## 安装步骤（两部分都需要）
1. 将 HF3.6.2 完整部署包覆盖网站工程，包括 `.streamlit/config.toml`，并在 Streamlit Cloud 执行 Manage app → Reboot app。
2. 核对网页设置中的构建号为 HF3.6.2-NATIVE-CAMERA-20260918。
3. 安装 GlobalHAB-Agent 安卓1.2.0 APK，可覆盖旧版，签名及包名相同，无需先卸载。
4. 重新打开应用 → 影像识别 → 现场拍照 → 点击选择文件按钮 → 打开系统相机拍照 → 拍摄并确认 → 返回网页看到“图片已收到” → 开始影像识别。
5. 可先用“快速规则筛查”检查照片回传；它不调用深度模型，不能替代藻种或毒素识别。

只装新版APK而未更新服务器时，旧网站的“现场拍照”仍可能显示原来的网页摄像头。这时可先选“上传图片”，点击选择文件后，新版APK也会提供系统拍照选项。上传既有图片的入口始终保留。

## 验证范围
已检查 APK 从清空的编译目录构建、签名和对齐、与旧版签名一致、拍照Provider打包、版本号3/1.2.0；照片文件接入页面后能执行快速筛查，普通网页上传和识别异常反馈测试通过；五工作区回归通过。
412像素宽的实际本地网页预览中：五个导航按钮上三下二排列，高48像素，页面宽与内容宽均412像素，无整体横向溢出。预览为网页，不是手机实机截图。
未连接三星S24 Ultra或安卓模拟器，未实测系统相机授权、取景、拍摄确认及完整回传；不能将编译通过等同于实机相机已验证。未自动部署线上；深度模型仍依赖服务器网络、权重及内存。

实现参考：Android 官方 ACTION_IMAGE_CAPTURE / EXTRA_OUTPUT 文档
https://developer.android.com/reference/android/provider/MediaStore#ACTION_IMAGE_CAPTURE
