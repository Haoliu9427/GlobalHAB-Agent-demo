# HF3.6 手机竖屏与识别反馈修复
构建号：HF3.6-MOBILE-20260918

配合安卓1.1.0使用。完整覆盖服务器工程并重启，右上角版本应显示上述构建号。未自动更新线上网站。

- 新增独立手机CSS：两列摘要卡、纵向科研图卡、两列工作区入口、单列表单与收敛间距；桌面布局保持原样。
- 影像识别按钮移到图片输入区，现场信息默认折叠但仍可填写。
- 图片上传后确认文件已收到；更换图片时清除旧结果，避免误读。
- 点击识别即显示阶段状态，模型首次加载或下载权重有明确提示；失败时显示错误及诊断，避免无反馈。
- 增加快速规则筛查入口，不下载深度模型，仅分析水色、纹理和质量；不冒充藻种或毒素识别。
- 修复热更新时影像模块可能保留旧代码的问题。

边界：没有保证深度模型服务一定可用，也未校准新的藻华分类器。模型下载、网络、内存和权重问题仍需按服务端错误排查。DEFER仍如实保留。

验收：本地快速筛查出结果；模拟模型异常显示错误且没有旧结果残留。412像素宽网页没有整体横向溢出。五工作区回归检查。手机实机相机、底部导航及完整深度识别仍需在设备上复测。
