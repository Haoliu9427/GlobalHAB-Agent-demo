# HF3.7.1 大模型规划与科学检验

本版以 HF3.6.2 为基础，保留蓝白首页、图表、手机两行导航、安卓系统拍照入口和原有数据。

进入“研究验证”，默认使用“大模型规划与科学检验”。基于规则控制器的规划与科学检验保留为规则控制器参照；首页历史曲线不会冒充大模型本次运行。首页各图卡的查看详情仍进入对应原版页面。

默认直接复用网站配置，只有主动勾选“本轮手动指定其他模型”才显示API输入框。研究问题和预算直接显示在页面。

## 使用已有网站模型配置
直接复用 Streamlit Cloud Secrets 的 [qwen] 配置：base_url、model、api_key。与现有远程模型解读使用同一套服务器设置。无需在聊天中发送密钥，也不需要写入部署包。如果之前只在“模型解读”页面临时填写过信息，则不是服务器配置，请在新页面重新填写，或由网站管理员配置 Secrets。

页面填写研究问题与实验预算，点击“开始大模型规划与检验”。一次候选或单项负对照计一次实验；默认8次，另有初始季节参照1次、冻结后的候选与参照2次拟合。统计实际拟合次数、耗时和模型服务报告的token；服务未报告时明确显示未知。重复或无效调用也占规划轮次，避免无限循环。

## 新流程
大模型根据研究问题和验证反馈输出结构化工具调用；程序校验后执行候选检验、反方向对照、时间置换对照，再把工具返回的数值交给大模型选择下一步。候选不再预先全部计算。模型调用采用兼容 Chat Completions 的JSON工具协议，不要求服务商支持原生function calling；没有把规则选择伪装成模型选择。

只有完成候选和两种对照后才能冻结；反证不支持时也保留负结果。工具白名单不允许大模型执行任意代码或访问其他文件。模型服务失败就保留错误状态和已经完成的记录，不自动切换为规则控制器。

所有候选使用相同日期、区域的验证行；时间先训练、再验证、最后测试。测试区域不进入训练与验证，最终测试仅在候选冻结后执行，不把测试结果送回规划模型。时间置换限制在各数据分块内部。大模型只收到研究问题、工具目录和聚合反馈，不收到完整原始观测或合成真值。

完成后可以下载本轮审计JSON，包括模型名称、数据哈希、划分、预算、工具参数、简短决策理由、实测反馈和冻结后的测试结果；密钥不写入下载记录。最终结果也进入当前会话的结果池，可在“模型解读”中继续查看。

## 适用范围
当前新增规划流程针对720天匿名合成任务，检验本地/上游、6个滞后与2种专业模型。滞后时间不是预警提前量，当日环境关联检验不能当作7—30天真实预报。本次没有把所有实海训练和影像工具都改成自动规划。
反方向与一次时间置换属于诊断性对照，不是多重置换显著性检验，不证明因果。选择本地候选时，反方向项是替代路径对照。固定季节参照用于检查流程，不是权威机构外部对比。本版没有证明优于贝叶斯优化、官方预警或节省token；这些需要同任务对照实测。

## 部署和验证
将完整包更新到原网站工程，保留网站现有 Secrets，执行 Manage app → Reboot app。设置中的版本号应为 HF3.7.1-LLM-SCIENTIFIC-20260919。安卓1.2.0联网读取网站，不需要为这次规划功能重新安装APK。
本地已通过预算与工具权限、反馈驱动循环、测试标签隔离、无静默回退、原有工作区与首页链接检查。自动测试使用明确标记的脚本规划器验证真实科学工具执行；不是大模型实际效果测试。本地未配置网站密钥，尚未完成真实远程大模型联调，也没有自动部署线上。更新后应使用已有模型配置运行一轮，检查真实工具调用与审计记录。
