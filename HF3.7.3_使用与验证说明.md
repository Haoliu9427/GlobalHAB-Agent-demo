# HF3.7.3 内置 Qwen 科研规划

已核对用户提供的 HF3.6.2 部署包。模型目录包含 Qwen2.5-0.5B、1.5B、3B Instruct，原有 foundation.py 通过 Transformers 在服务器加载它们，用于预测评分。这与 remote_qwen.py 的 [qwen] API 配置是两条不同路径。此前将内置模型入口绑定到 Secrets 是接入错误，本次修正。

研究验证提供三个选择：
1. 使用网站已有模型（qwen）：从原工程同一模型目录选择 Qwen，通过 Transformers 在服务器生成工具方案，无需 API Key。已下载权重使用 Hugging Face 缓存；规划器缓存最近加载的一个模型，并用锁串行运行，避免会话同时生成。
2. 自行配置 API：使用用户单独提供的地址、模型和密钥，不读取内置模型或混用服务器密钥。
3. 基于规则的科学检验与验证：保留已有规则研究流程。

内置模型原先只计算预测分数，本次增加了结构化文本生成，送入既有科学工具执行循环。非法方案会被拒绝，轮次有上限；不保证小模型每次都能完成所有检验，不以规则控制器兜底冒充大模型。

部署：更新完整包，保留 requirements.txt，重启 Streamlit Cloud。确认版本 HF3.7.3-LLM-SCIENTIFIC-20260919。首次加载可能下载权重，CPU生成可能较慢，3B模型需要明显更多内存；建议先以0.5B确认可用。无需配置 [qwen] Secrets 即可选择内置模型，但仍需要安装依赖、取得权重以及足够服务器内存。APK无需重装。

已检查三个模型目录、内置/API分支隔离、依赖缺失提示、工具预算与数据隔离。当前本地预览缺少torch、transformers及Qwen权重；没有执行真实Qwen生成，也未确认云端权重是否已经缓存、服务器是否有足够内存。部署包中的模型目录和加载代码不能证明线上模型已经常驻。这一限制不应隐藏或用假结果代替。

原始科研数据、历史输出、手机导航及相机入口保持不变。新规划仍是匿名合成科学检验，并非真实海域7—30天预警。
