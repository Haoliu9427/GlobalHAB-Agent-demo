# HF3.9 模型选择与科学检验

运行版本：HF3.9-MODEL-SELECTION-20260920。入口 app.py 或 streamlit_app.py 均可，须部署本目录完整内容。

## 本次实现
完整目录37个条目；当前合成阻断验证任务接入14个表格预测模型和STS-Interaction GLM，共15个。其他22个明确说明未接入或参照用途，原数据分析工作区仍保留。
大模型先通过select_models选2–3种预测模型，再逐个调用实际训练和验证；允许随后追加候选。冻结前须至少比较两种不同预测模型，并完成冻结候选的方向和时间置换对照。独立测试集不提供给规划器。

## STS范围
复用user_engine.sts_arrays的交互项构造，用训练集拟合的标准化与逻辑回归计算结果。当前路径和滞后的合成信号作为传入信号，训练期尺度变换后生成输运门控、营养盐及交互项。只使用该函数的交互项输出，不使用序列分支。此为单时刻合成任务适配，不能冒充完整STS-Gated TCN或真实海流机制；STS-Gated TCN不在本轮可调用目录。

## 网站管理员配置一次
在Streamlit Secrets设置以下内容，填入自己的Token；访客不填写密钥：
```toml
[qwen]
base_url = "https://api-inference.modelscope.cn/v1"
api_key = "在此填写你的魔搭Token"
model = "Qwen/Qwen3.5-35B-A3B"
models = ["Qwen/Qwen3.5-35B-A3B", "Qwen/Qwen3.5-122B-A10B", "Qwen/Qwen3.5-397B-A17B"]
```
不要重复创建已有[qwen]段，请合并字段。真实Token不在本工程包中。模型列表是管理员提供的服务目录，配额和当前可用性以魔搭返回为准。

## 实际验证
15个适配器均完成真实拟合。另验证了首轮选择约束、不同模型比较要求、负对照与冻结要求，以及改变独立测试标签不影响验证分数。
Qwen3.5-35B-A3B通过Streamlit AppTest的真实模型调用，选择Logistic、STS-Interaction GLM、Random Forest，完成3个候选和2种对照，冻结后独立测试。最终AP约0.171，季节参照约0.176；保留负结果。该测试不是脚本替代规划，也不证明科研效果优于基线。
397B本轮遇到魔搭HTTP200但choices为空的响应；针对明确0token空响应仅重试一次，仍失败则停止，不用规则结果替代。
未在Streamlit Cloud实际部署验证。更改部署入口和Secrets后请核对运行版本。

## 视觉与保留功能
平台和自选API模式共用规则页面的图表字体、配色、图卡与折叠明细组件；增加候选选择理由、独立测试对比、负对照与探索轨迹。保留原有首页、规则检验、影像、数据分析和Android源码。APK本次未重新编译。
