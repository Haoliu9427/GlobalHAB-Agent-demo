# GlobalHAB-Agent 参考图高保真 UI 重构（2026-09-18）

本轮以已确认的“项目总览”参考图为结构基准，重新组织展示层，而不是只修改颜色。

## 首页结构

- 顶部产品导航与品牌区改为横向轻量导航，项目总览为蓝色选中态。
- 项目总览页按参考图固定为：标题/海洋横幅 → 4 个 KPI → 研究思路概览 + Case 状态 → 工作区联动 → 3 个分析面板 → 挪威前向验证。
- 研究思路概览压缩为五步：多源观测 → 路由与异常识别 → ST/STS 科学推理 → 证据落地与验证 → 迁移性评估。
- 南澳真实事件回放改为真实 qPCR 记录的站点 × 日期矩形热力图。
- 环境因子与迁移验证矩阵只显示现有输出中的真实指标：CTE、净方向性、Spatial Durbin 间接效应、南澳 K. cristata 检出占比、挪威长期前向 AP 等；缺失组合显示为“—”，不以人为分数补齐。
- 挪威验证改为紧凑矩阵，保留模型 AP、参考模型 AP、季节基线 AP 和四个前向时间窗。

## 全站统一

- 所有工作区统一海洋蓝/青色、白色面板、细边框、小圆角和低阴影。
- 默认侧栏折叠，避免首页被挤窄；侧栏仍保留地图、Case 与研究页运行参数。
- Tabs、上传区、按钮、数据表、指标卡、Plotly 图表和地图容器统一样式。
- 保留原科学计算、模型、Case、现场影像和 LLM 解释逻辑；本轮不改科学结果生成方式。

## 验收文件

- `validation/high_fidelity/homepage_preview_1448.png`：使用本轮 HTML/CSS 结构和真实项目数值生成的 1448px 宽静态布局预览。
- `validation/high_fidelity/high_fidelity_checks.json`：结构与数据绑定检查。
- `scripts/check_high_fidelity_ui.py`：可重复执行的 UI 结构检查脚本。

静态预览用于验证结构、比例和信息密度；实际 Streamlit 中三个分析面板仍使用 Plotly，因此保留悬停提示和响应式交互。
