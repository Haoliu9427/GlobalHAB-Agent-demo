# 统一OpenStreetMap地图
所有地图统一通过src/globalhab_demo/map_style.py渲染为Plotly Scattermap，底图open-street-map，不需要Mapbox Token。保留底图自带署名。全球概览默认全局视野，局部观测按坐标范围自动选择中心和缩放；支持拖动缩放。
覆盖风险研判、真实事件回放、生物响应沙盘、中国近海验证、训练数据覆盖和现场验证地图。原有坐标、风险色阶、数值、分类与悬停内容保留，标记统一为圆点。原有固定文字标签改为悬停。
底图依赖联网访问瓦片服务，不保证离线使用或大量并发访问；生产大规模使用需评估瓦片服务容量和使用政策。
已通过地图对象生成与坐标/色标值保留检查、Python编译；未完成浏览器瓦片加载与视觉验收。此次无新增预测实验。
整包同步，侧栏标识Ocean OSM 2026.09.14。必须上传map_style.py及assets/interface.css。
