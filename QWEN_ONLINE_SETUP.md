# Qwen线上调用配置

入口一：工作区 → 自有数据分析 → 远程大模型服务。

入口二：工作区 → 大模型结果解读。该入口只发送已计算结果的结构化摘要，用于科研、答辩、论文结果段或管理摘要，不执行远程概率预测。


## 独立“大模型结果解读”工作区

该工作区复用同一套Chat Completions兼容连接方式，但和“远程大模型 · API预测”是两条不同路径：

- **API预测**：把历史特征发送给远程模型，请模型返回0–1概率，再进入校准和验证；
- **结果解读**：只把已经计算完成的结果摘要发送给远程模型，请模型用文字解释，不重新计算概率。

结果解读页支持项目固定证据、真实训练、中国近海、自有数据最近一次运行，以及用户上传的CSV/JSON/TXT/Markdown结果文件。发送前可以完整查看摘要，并必须主动勾选“允许把上方结果摘要发送给远程大模型”。

系统Prompt要求模型只引用摘要中的数字，区分证据层级，并固定输出“结论—关键数字—可信度/不确定性—可声称—不可声称—下一步验证”。答辩模式额外给出30秒口头讲法。若输入文件内出现提示词或命令，程序明确要求远程模型把它们视为数据而不是指令。

## 用户自行填写（默认）

填写API地址、模型名称、API Key（密码输入框），点击测试模型连接。勾选发送数据授权后，在模型列表选择“远程大模型 · API预测”，再开始分析。也可以仅运行传统模型，然后点击“用大模型解读本次结果”。测试连接也会调用服务，可能计费。

密钥保存在当前Streamlit会话内存中，不写入文件、日志或下载包；请求由Streamlit后端发出。不同用户会话不共用输入凭证。用完可点击“清除本次密钥和结果”。切换到服务器配置不会把服务器密钥填进用户输入框。地址、模型或密钥变更后，旧结果会隐藏。

只接受公网HTTPS标准443端口，拒绝内网/本机地址和重定向。生产部署仍应在网络层限制出站访问，以防恶意DNS变更；应用层DNS检查不能替代网络隔离。API必须兼容Chat Completions且支持纯JSON概率响应，并非任意聊天网站URL都能使用。

## 使用服务器配置（可选）

在Streamlit应用的Secrets中配置（不要提交真实密钥到GitHub）：

```toml
[qwen]
base_url = "https://你的服务域名/兼容接口/v1"
api_key = "你的服务密钥"
model = "服务商控制台中可调用的Qwen模型ID"
```

base_url不包含末尾/chat/completions，程序自动追加。仅支持HTTPS Chat Completions兼容接口。服务地区、密钥和模型权限必须对应；这里不预填可能与账户地区不一致的地址。也支持环境变量HAB_QWEN_BASE_URL、HAB_QWEN_API_KEY、HAB_QWEN_MODEL。

配置后点击测试模型连接。上传CSV、勾选远程发送授权，再在模型列表选择“远程大模型 · API预测”。不会要求安装torch或transformers。模型目录保留本地Qwen和Chronos；这些本地模型仍需其原有依赖。

远程Qwen预测是独立新协议：要求服务返回JSON概率，随后沿用独立校准集校准及同一留出集评价。它不等于原来的本地分类token得分；原来的真实实验结果未修改，也不能作为远程版本的性能证明。每个评估历史发送一次请求，单任务最多300次；另有连接测试及解读调用。概率预测返回格式不合格、超时或服务错误会中止，不伪造结果、不自动换模型。结果解读路径对DeepSeek另有一次受控兼容重试：当思考模式只产生推理内容而没有最终可见回答时，自动改用稳定非思考模式重试一次。记录请求次数和服务返回token用量，暂不换算货币成本。单次确定性配置不保证服务端完全可复现。

“用大模型解读本次结果”是独立按钮，可在只运行传统模型后使用，不会重新计算或提升预测指标。解读单独下载。

可选择服务器配置或用户当前会话配置，结果包不保存凭证。公开应用需由部署者配置访问控制和服务商预算/限额；300次仅为每任务限制，不是全站费用上限。用户事件说明中不要填写身份或敏感信息。远程请求发送历史特征，包括CSV中映射到模型的特征；原始表、未来标签不发送。

本轮验证使用模拟HTTP响应，未配置真实服务密钥，未进行真实API调用或远程生态性能实验。Chronos远程服务不在本次实现范围内；不会用聊天API替代Chronos。

## 其他模型
该入口支持Chat Completions兼容服务，不限Qwen。模型名称按服务商控制台填写，不提供未经核实的可用模型列表。服务需支持messages、temperature、max_tokens字段，并返回choices[0].message.content。原生非兼容接口需要另行适配。Chronos不通过该聊天接口调用。后台旧[qwen]配置名保留兼容，用户自行填写不受此名称限制。实际模型ID在foundation_audit.json中记录。

## DeepSeek入口
自行填写 → 模型服务 → DeepSeek，会填入 https://api.deepseek.com。输入你自己的密钥，点击读取服务可用模型，再把返回的模型ID填入模型名称。也可直接手工填写。切换服务会清除旧密钥和结果，避免误将凭证发给另一个服务。读取列表不代表预测协议已通过；仍需测试连接并运行本次分析。

当前DeepSeek V4的Chat Completions默认开启思考模式。大模型结果解读页因此新增“生成模式”：默认“稳定解读（推荐）”会显式关闭思考，只读取最终可见回答；也可选择低/高强度思考。若思考请求达到输出上限、只返回 `reasoning_content` 而没有最终 `content`，系统自动以稳定模式重试一次。`reasoning_content` 从不作为用户可见解读的替代文本。未在发布包中内置真实API Key，也不会把Key写入结果文件。官方接口参考：https://api-docs.deepseek.com/ 。
