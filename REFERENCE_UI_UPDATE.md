# 参考图布局更新

基于 GlobalHAB-Agent_HF2_仓库根目录直接部署版_20260918.zip。
构建标识：HF2-REFINED-20260918。

## 页面调整
- 项目总览按参考图重新调整海景标题区、四项指标、研究流程、Case环图、工作区入口、三列结果图及底部挪威验证表的比例。
- 流程图标改为悬浮圆形图标，工作区卡片提供实际跳转。
- 取消主页图表的固定高度裁切，图表仍来自工程结果，未使用参考图中的示例数字。
- 五个工作区统一蓝白卡片、字号、边框及间距；移除旧样式对部分卡片施加的790/980像素最小高度，避免大片空白和双层边框。
- 研究与验证：总览合成指标默认折叠，各研究模块直接进入任务区；方法长说明按需展开。
- 自有数据分析：突出上传、预测任务和模型运行；远程服务设置默认折叠，Qwen、DeepSeek和自定义入口仍可用。
- 现场影像甄别：简化页头，保留影像、数据、训练三个入口。
- 大模型结果解读：简化页头，完整摘要改为展开查看，去除重复600字预览。
- 窄屏保留横向导航/流程滚动，指标和主要分区逐步转为单列。

## 数据与功能
本次修改展示层与导航，并修复南澳主页热图日期轴的显示问题，没有更改模型、训练方法、指标计算、原始数据或保存的科学结果。
原有数据文件、验证输出和依赖文件保留。南澳主页热图不再将未采样单元显示为零，保留空白；站点采用S编号，在悬停时显示原名。未向GitHub或线上Streamlit写入。

## 部署
解压后将包内内容放到仓库根目录，入口仍是 app.py，勿再嵌套同名目录。
依赖文件没有改动。更新后可在 BUILD_ID.txt 确认构建标识。

## 检查
五个工作区均经过 Streamlit AppTest 启动检查，未出现应用异常；记录在 validation/reference_ui_smoke.json。
启动检查不等同于真实API调用或模型重新训练。浏览器视觉检查结果另见 validation/reference_visual_check.json（若存在）。
