# HF3.9.7 首页联动与发布修复

## 修复内容
1. 恢复 HF3.6.2 首页页面结构和原始 outputs 结果文件，包含探索曲线、传播时差、挪威前向验证和中国近海检验。沿用已有结果，不伪造本轮模型输出。
2. 首页研究入口直接进入规则驱动科学检验；详情链接保留对应栏目。
3. 页面源码按版本完整加载，不再使用旧模块中保留的研究表单；没有字符串替换源码。自主接入仍使用“保存并检查连接”，无需重复填写 Secrets。
4. 发布检查不再错误要求版本包含 4.1，而是核对 app.py、BUILD_ID.txt、VERSION.md 和页面模块版本一致，并检查首页关键结果文件存在。保留自动测试。
5. 保留图表对齐、结果同步及安全的连接错误提示。

## 更新步骤
将整个包的内容同步至 GitHub 仓库根目录，尤其不要漏掉 outputs、src、scripts、tests 和 .github。
Streamlit 管理界面执行 Reboot app；会清空未下载的会话结果。版本应为 HF3.9.7-HOME-RELEASE-20260920。
自主接入页面会显示相同的页面版本，且有“保存并检查连接”按钮。现有 Secrets 无需修改。

## 测试结果与范围
发布文件检查通过。原 CI 的 7 项测试通过，新增首页数据、旧模块兼容及安全错误提示的 3 项测试通过。
浏览器预览首页恢复 6 张图；自主接入显示新表单。
Streamlit AppTest 验证首页关键数字、详情导航到规则模式、旧模块滞留时仍显示新表单。
历史真实 Qwen 结果回放验证图表、下载、模型解读同步。模拟 DeepSeek 的成功和 401/402/404/429/503 响应验证表单流程；没有使用用户 DeepSeek 密钥，也未验证该账号的权限或余额。
GitHub 原失败运行：35460178537；失败步骤为 Verify required release files。更新后需由 GitHub 再运行确认云端状态。
