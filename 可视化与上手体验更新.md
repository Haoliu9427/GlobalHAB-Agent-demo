# 可视化与上手体验更新

基于全模块卡片与图表优化完整部署包。

首页：三张图表卡片统一宽度与高度，标题、绘图区、注释采用同一结构。南澳灰格说明压缩成一行。环境因子恢复图形表达，以独立尺度的三组小图展示传播方向、邻区关联和挪威排序表现，含义置于悬停提示，不把不同指标的长度直接比较。

生物响应：默认地图仅含网箱养殖海区，与下拉选项一致；完整渔业背景放入独立折叠地图，不将捕捞与贝类区域套入网箱参数。

科学解释：突出多尺度异常曲线与异常日；高亮实际峰值时滞；关联比较使用零点参考的分组点图。探索验证：增加逐步实验轨迹，完整Benchmark改用排名点图。真实验证：逐年AP均值及种子标准差图，保留原指标和完整明细。

研究助手：默认使用可下载的4站点×420天合成示例。无需CSV或照片，可依次点击生成计划、确认方案、执行分析。全过程明确合成数据来源；不是新增真实海域验证结果。上传自己的数据仍保留，照片可选。只有照片可使用影像识别工作区。

模型解读：全宽顺序卡片，选择结果、设置解读、连接服务；保持远程发送的原确认机制。

验证：五工作区启动检查通过；无上传示例已完成计划确认及真实模型执行；1448×1086浏览器测量首页三图卡片高度均为380px。

部署：完整上传仓库根目录，入口app.py，重启Streamlit。构建号HF2-VISUAL-20260918。未修改远程仓库。
