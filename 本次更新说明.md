# 图形重排版 · HF2-VISUAL-REFINED-20260918

在“多模态与结果协同版”的基础上重做图形表达，保留上一版功能和原始实验数值。

- 研究思路：五段环形箭头直接首尾相接，取消外围细线箭头，保留模块跳转。
- 首页三卡：缩小卡片固定高度并增加图表占比。环境因子与迁移验证分成“传播时差”“真实验证”两个页签。
- 中国近海：三个海区分别显示模型与基线成对条形，统一AP横轴，用颜色和差值标明提升/下降。删除额外解释展开框，任务、年份、种子范围放入悬停。不同分子标记依然分别评估。
- 网箱鱼沙盘：压力时间热图、带敏感性包络的横向峰值压力图、摄食机会横向图。避免多条线及重合气泡标签。
- 探索验证：基线横向对比；单步试验点与累计最佳阶梯线；风险概率曲线与实际事件/排名报警两行标记，避免把事件二值标签画成连续折线。
- 本次修改未重训模型，未改变结果或将合成结果改为真实验证。

已做Python编译、五个工作区启动与浏览器页面检查。
完整同步仓库根目录即可，Streamlit入口仍为app.py。没有更改GitHub或线上部署。

---

# HF2 多模态与结果协同版

版本标识：HF2-MULTIMODAL-20260918

## 新增入口与使用方法
- 研究验证 → 生物响应沙盘 → 分析对象：网箱鱼、贝类养殖、海藻养殖、捕捞资源。
  网箱鱼仍使用原有生理响应科学原型；其他三类为环境输入与用户阈值对照，不是物种耐受模型、毒素预测或死亡率预测。默认阈值只用于演示。
- 数据分析 → 研究助手 → 使用示例数据：生成计划、确认、执行，输出AP、Brier和ECE三张对比图。完整结果与审计信息收在展开项中。可选合成水色图同时体验影像筛查。
- 影像识别 → 示例体验：三种合成水色图，可以直接分析。图片不是实海照片，不能用来证明藻华识别准确率。
- 影像识别 → 视频筛查：上传拍摄好的MP4/MOV/AVI，最多80MB、2分钟，均匀抽取最多12帧，显示异常指数时间线与各帧结果。不是实时录像或连续目标跟踪；规则水色异常不等于藻华确诊。
- 模型解读 → 本会话结果汇总：收集研究助手、自主分析、风险情景、科学解释、真实数据验证/回放、网箱鱼沙盘与影像视频结果。其他对象的环境情景通过“记录本情景供模型解读”加入。勾选多份结果，设置模型并确认后生成综合解读。暂存最多30份，下载JSON保存；会话结束或应用重启可能清空。不会自动发送API请求。
- 首页：五扇形加方向箭头；三张结果卡片底部说明移到悬停提示；传播时差曲线与监测资源覆盖对比替代密集指标；中国近海按跨年测试展示模型与基线，保留性能下降结果。

## 部署
将压缩包内的完整目录内容同步到GitHub仓库根目录，入口仍为app.py。
本包增加opencv-python-headless用于视频解码，务必同步requirements.txt并等待Streamlit重新安装依赖。没有向GitHub或线上应用写入。
现有远程模型API地址、模型名称、密钥设置方式保留。没有实际API凭据时无法验证远程模型调用成功率。

## 验证
validation/reference_ui_smoke.json：五个工作区的启动检查。
validation/demo_entry.json：免上传示例完整执行检查。
validation/multimodal_features.json：三种合成水色、12帧视频抽样、三种新增对象切换检查。
本地浏览器核对了首页卡片高度、示例影像入口和结果汇总入口。
这些是功能验证，不是新增模型的真实海域性能验证。
