# 界面更新说明

基于 HF2 界面导入修复完整部署包继续修改，构建号 HF2-COASTAL-20260918。

1. 科学Agent、Agent探索轨迹去除空格。
2. 删除英文标语；五个入口采用四字名称，并调整导航、搜索、设置位置。
3. 当前候选改为发现的传播线索，以顺流传播及上下游信号时间差解释合成实验结果。
4. 首页底部挪威表格替换为中国近海跨年检验配对点图，三种子均值与基线同图呈现，保留负结果。挪威结果仍保留在其余验证页面。
5. 中文展示适配器覆盖文本、表格与图表；模型内部标识及下载证据保持原始格式，不改变算法输入。
6. 研究区七模块使用图标、说明与选中状态的卡片导航，其他工作区沿用一致卡片样式。

中国近海图：outputs/china_mainland/{ITS1,18S_V4}/metrics.csv。2019—2020训练，2021测试；验证目标为藻种检出识别，不是提前预警。渤海暂无对应跨年测试。

部署：解压后将全部内容放入仓库根目录，入口 app.py。必须同步 src、assets、data、outputs 等完整目录。用户自行更新 GitHub；未执行任何远程写入。
