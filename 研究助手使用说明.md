# 研究助手与多模态证据

入口：首页 → 数据分析 → 研究助手。原有全部模型及Qwen、DeepSeek、通用远程服务配置保留在同页的“自主配置”。

## 操作
填写研究目标，上传CSV；可同时上传海面照片、填写站点编号和拍摄日期。确认预测时效与最多模型数，填写事件阈值、物种、value含义。点击“检查数据并生成计划”，确认方案后执行。修改输入会隐藏旧计划和结果，避免错配。结果页包括分析结论、执行轨迹、多模态证据；可下载本次任务证据包。

## Agent实际执行能力
受约束的规则规划器识别历史异常或预测需求，检查所述时效与参数是否一致。数据不足时提出历史异常筛查方案；用户确认后才执行。预测模型候选为Logistic、HistGradientBoosting、Extra Trees。前两类模型必评，第三类根据验证AP相对常数基线的提升、ECE和三种子AP跨度触发。门槛分别为0.05、0.10、0.03，是工程调度规则，不是已验证的科学显著性阈值。
只读取训练和验证窗口来选择；模型按验证AP排序，平分时比较Brier、耗时。冻结后复用原工程预测引擎，独立校准并以三个种子在相同测试集比较季节基线。单候选失败会记录并继续；全部失败则停止。未来输出沿用原项目“第N到N+3天首个采样事件”定义，起报日期为各站点最新有标签观测，不保证是今天。

## 多模态的准确定位
文本定义目标，时间序列支持预测验证，影像提供质量与视觉异常证据。按站点、日期匹配；无法匹配时明确列为独立证据。若照片日期对应预测起报日，联合显示该站点概率和视觉复核优先级；两者不做数值相加，不互相确证。研究助手当前使用已存在的规则视觉筛查器；原影像识别工作区保留自适应视觉分支。不是端到端图像与时间序列联合训练，不宣称多模态提高预测准确率。

当前助手不调用大语言模型进行自由规划，不是通用对话机器人。无需远程API即可执行；不会将用户观测或影像发往远程服务。自主配置的远程模型仍遵循原配置与确认流程。

## 复核与部署
构建号HF2-AGENT-20260918。测试使用合成软件测试样本，仅用于确认执行、封存、证据关联和回退路径，不是新增科学结果。完整上传仓库根目录后重启Streamlit，入口app.py。不需要改动数据库或API凭证。
