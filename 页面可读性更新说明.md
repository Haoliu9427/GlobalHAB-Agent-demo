# 页面可读性更新

基于扇形概览与任务入口完整部署包，构建号HF2-CARDS-20260918。

1. 首页研究概览卡片缩窄，扩大扇形图在卡片中的占比；任务入口保持独立。
2. 南澳热图采用横向对数色标。浅灰表示没有有效浓度记录，浅绿表示记录浓度为0；不把缺失值填成0。显示的是站点与日期子集，同站点同日取最大值；悬停可查站点名称、日期和浓度。
3. 环境因子与迁移验证改为三张解释卡。分别标出合成实验的时滞信息、合成模型的间接关联、挪威真实数据的AP；取消混合单位的归一化颜色强度。间接关联原值乘100，按概率百分点展示，每1标准差输入变化。
4. 真实事件回放、生物响应沙盘、科学解释、探索与验证、数据来源与复核、真实数据训练与验证均增加分组大卡片；图表使用内层卡片，详细数据默认折叠。
5. 候选与基线、真实模型对比采用点图；真实模型展示原有三种子标准差，未改变评价指标或算法。

全部五个工作区启动检查通过。部署需同步整个包，入口app.py，更新后重启Streamlit。
